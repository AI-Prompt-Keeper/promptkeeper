# Deploy Prompt Keeper backend to Fly.io on every push to main when backend/, service/, or console/ changes.
# Required: Repository secret FLY_API_TOKEN (create with: fly tokens create deploy -x 999999h).

name: Fly Deploy

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - 'service/**'
      - 'console/**'

jobs:
  deploy:
    name: Deploy backend to Fly.io
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Fly CLI
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Deploy to Fly.io
        run: flyctl deploy ./backend --remote-only
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
