{
  "name": "@promptkeeper/sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for Prompt Keeper proxy — drop-in style for OpenAI with function_id, variables, and optional fail-open.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["promptkeeper", "openai", "llm", "proxy", "sdk"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "files": ["dist", "README.md"]
}
